<UserControl x:Class="ChatApp.Views.LoginPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <!-- Tiêu đề trang đăng nhập -->
            <TextBlock Text="Login" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" />

            <!-- Nhập tên người dùng -->
            <TextBlock Text="Name:" FontSize="16" Margin="0,0,0,5"/>
            <TextBox x:Name="NameTextBox" FontSize="14" Margin="0,0,0,20"/>

            <!-- Nhập địa chỉ IP -->
            <TextBlock Text="IP Address:" FontSize="16" Margin="0,0,0,5"/>
            <TextBox x:Name="IpTextBox" FontSize="14" Margin="0,0,0,20"/>

            <!-- Nút đăng nhập -->
            <Button Content="Login" FontSize="16" Padding="10" Margin="0,20,0,0" Click="LoginButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>
